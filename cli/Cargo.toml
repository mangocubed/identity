[package]
name = "identity-cli"
version.workspace = true
edition.workspace = true

[dependencies]
clap = { version = "4.5.48", features = ["derive"] }
tokio = { workspace = true }
identity-core = { workspace = true, features = ["server"] }
